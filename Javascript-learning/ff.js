/*
console.log("Java script learning")
function fruits(apples , oranges) {
    const juice = `Juice with ${apples} apples and ${oranges} oranges.`;
 
    return juice;
}
console.log(fruits(4,5));

function year(birthYear) {
    return 2030-birthYear ;
}
const day = year(prompt("enter your birth year"));
console.log(day);

const age = birthYear => {
    const age2 = 2033 - birthYear;
    
}
//distributing toys to children by using functions
/*function toy(boys,girls){
    return `There are ${boys} boys and ${girls} girls in a classroom`;
}
console.log(toy(20,30));

//Creating objects in Javascript
const tab =  {
    firstName : 'Uman',
    lastName : 'Tabassum',
    age :2000,
    job : 'scientist',
    friends : ['Gulshan','Sana','Firoz'],
    hasDriversLicence: false,
    cal: function(age){
        console.log(this)
        
        return 2030-this.age;
    }

};
//Retrieving data from an object..
//console.log(tab)
//console.log(`${tab.firstName} has ${tab.friends.length} friends and her best friend is ${tab.friends[0]}` );
//Operator precedence is applied here for dot  operation first the javascript goes with tab.friends (left-right) next 
//it deals with array as tab.friends.length okk calAge
console.log(tab.cal())
console.log(`Jonas is ${tab.cal()} years old and he has ${tab.hasDriversLicence ? 'a' : 'no' } driver's licence` );
//if(this.age>40){
//console.log(`Jonas is ${tab.cal()} years old and he has a driver's licence`)
//}
//else
//{
//    console.log(`Jonas is ${tab.cal()} years old and he has no driver's licence`)
//}

const mark = {
    firstName : 'Mark',
    LastName:'Miller',
    mass: 78,
    height:1.69,
    bmi : function(){
        console.log(this)
        this.calBmi = this.mass/this.height**2;
        return this.calBmi;
    }
};
const john = {
    firstName:'John',
    lastName:'Smith' ,
    mass1:92,
    height1:1.95,

    bmi: function(){
        this.calBmi= this.mass1/this.height1**2;
        return this.calBmi
    }
};
console.log(`🏋️${john.firstName} BMI is ${john.bmi()} is higher than ${mark.firstName} BMI which is ${mark.bmi()} `)

for(let rep=1; rep<=10 ; rep++){
    console.log(`The weight lifting repitition is ${rep} 🏋️`)
}*/
const col =function(bill){
    return bill >= 58 & bill <=
}
const  bills =[22,295,176,440,37,185,18,1188,86,52]
const tips =[];
const totals =[];

console.log(object)
const x=5;


console.log(x)
if ( x==0){
    console.log("print");
}
c


